---
# @see https://prometheus.io/docs/alerting/latest/alertmanager/

global:

  # By default we use the default Debian Exim4 server which allows sending on localhost
  smtp_from: "alertmanager@localhost"
  smtp_smarthost: "localhost:25"
  smtp_hello: "localhost"

  # FIXME: This is definitely not good for production use, ok for local testing only
  smtp_require_tls: false

route:
  receiver: "default"
  group_wait: "30s"
  group_interval: "5m"
  repeat_interval: "4h"

  routes:

    # Node which matches watchdog alerts
    - receiver: "watchdog"
      continue: false
      matchers:
        - alertname="Watchdog"
        - severity =~ "warning|critical"


receivers:
  - name: "default"
    email_configs:
      - send_resolved: true
        from: "alertmanager@localhost"
        to: "baptiste@localhost"

  - name: "watchdog"
    webhook_configs:
      - url: "http://localhost:9094"
